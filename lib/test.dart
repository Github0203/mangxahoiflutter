import 'package:flutter/material.dart';

class TestNe extends StatelessWidget {
  const TestNe({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
        child: const Text(
      'Home',
      style: TextStyle(color: Colors.yellow),
    ));
  }
}
